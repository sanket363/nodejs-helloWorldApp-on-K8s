apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodjs-hello.fullname" . }}-test-connection"
  labels:
    {{- include "nodjs-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodjs-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
