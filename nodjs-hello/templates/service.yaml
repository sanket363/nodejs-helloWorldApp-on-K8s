apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodjs-hello.fullname" . }}
  labels:
    {{- include "nodjs-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodjs-hello.selectorLabels" . | nindent 4 }}
